<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="by.androidacademy.firstapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" /><!--for JobIntentService-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.activities.MoviesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".androidservices.threads.ImitationService"
            android:enabled="true"
            android:exported="false" />

        <service
            android:name=".androidservices.threads.DownloadIntentService"
            android:enabled="true"
            android:exported="false" />

        <service
            android:name=".androidservices.threads.DownloadJobIntentService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <service android:name=".androidservices.downloads.DownloadService"/>
        <receiver android:name=".androidservices.downloads.CompleteReceiver" android:exported="true">
            <intent-filter>
                <action android:name="by.androidacademy.firstapplication.androidservices.downloads.DOWNLOAD_COMPLETE"/>
            </intent-filter>
        </receiver>


    </application>

</manifest>